apiVersion: v1
kind: Service
metadata:
  name: crud-service
  namespace: study-smart
spec:
  selector:
    app: crud-service
  ports:
  - port: 3003
    targetPort: 3003
    name: http
  type: ClusterIP
