apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: study-smart
spec:
  selector:
    app: payment-service
  ports:
  - port: 3001
    targetPort: 3001
    name: http
  type: ClusterIP
